form:
  #template: default
  sections:
    # Section 1
    - section: Informations sur la demande
      inputs:
        - #----------------------------------------
          name: test
          if: 
            server: ($.ne_qc === 'oui')
            client: 
            all: 
          type: group
          label: Label
          help: "Choose your hand-packed pints whipped up by our expert servers :)"
          addLabel: + Ajouter une saveur
          validations:
            required: trim
          repeatable: true
          inputs:
            - #----------------------------------------
              name: flavor
              type: select
              label: Saveur
              help: TEST
              validationName: La saveur
              validations: 
                required: trim
              options: { vanilla: 'Vanilla', chocolate: 'Chocolate', strawberry: 'Strawberry', pineapple: 'Pineapple'}
            - #----------------------------------------
              name: quantity
              type: number
              label: Quantité
              validationName: La quantité
              validations: 
                required: trim
        - #----------------------------------------
          name: quantity
          type: number
          label: Quantité
          validationName: La quantité
          validations: 
            required: trim